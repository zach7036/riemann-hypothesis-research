# Change log

## v0.3 - August 12, 2026 - publication-audit draft

- Consolidated the full arbitrary-parameter rank--trace consequence into the master scalar inequality.
- Added closed-form low-multiplicity profiles for both zero-multiset mass and distinct zero locations.
- Added exact-multiplicity upper bounds for every fixed integer `j>=2`.
- Added scalar-relaxation sharpness witnesses and verified all parameter ranges `c>0`.
- Added an explicit proof that the off-line-pair weight in the `r>=3` sharpness witness is positive.
- Replaced floating-point headline values with exact rational interval certificates and directed rounding.
- Added an independent symbolic, stress-test, and discretized semi-infinite linear-program audit.
- Added source-level Lean formalization map pinned to upstream commit `3635e74826a4c1fcece7d1cd2b6fa75e43a00510`.
- Corrected the bibliography and separated conditional higher-correlation results from unconditional large-multiplicity results.
- Quarantined the amplitude, short-interval, Nyquist, and Hankel branches as exploratory until their stated analytic or observability obligations are closed.

## v0.2 - August 12, 2026 - exact-certificate draft

- Certified the Montgomery--Taylor constant and all displayed decimal bounds by standard-library-only rational interval arithmetic.
- Added tables through cutoff `r=100` and exact multiplicity `j=100`.

## v0.1 - August 12, 2026 - initial manuscript

- Derived the first low-multiplicity hierarchy from the released multiplicity-aware rank--trace family.

## 0.2 - 2026-08-12

- Replaced the asymptotic shorthand with an epsilon-form arbitrary-`c` master proposition.
- Audited the dependency chain against upstream commit `3635e74826a4c1fcece7d1cd2b6fa75e43a00510`.
- Added the exact interval certificate and an independently written symbolic audit.
- Made the total multiplicity identity and all status limitations explicit.
- Separated the xi-prime optimization from the zeta manuscript.
- Expanded the manuscript to 13 pages and visually preflighted the rendered PDF.
