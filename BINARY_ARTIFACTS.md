# Rendered artifacts

This GitHub source snapshot stores manuscripts and audits in reproducible text form rather than committing generated PDF binaries.

Build the principal manuscript locally with:

```bash
./scripts/build_paper.sh
```

The source package used to initialize this repository also contains rendered PDFs that passed visual preflight. They may be attached to a future versioned GitHub release after external review. The mathematical claims do not depend on the rendered binaries.
